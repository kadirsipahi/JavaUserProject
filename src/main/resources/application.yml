server:
  port: 8080

spring:
  datasource:
    url: jdbc:h2:mem:mydb?autoReconnect=true&useSSL=false&createDatabaseIfNotExist=true
    username: sa
    password: password
    driverClassName: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create
